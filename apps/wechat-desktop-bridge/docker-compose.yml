# WeChat desktop bridge: one stack per WeChat account, one custom channel per
# stack. See README.md for box setup and docs/architecture/channels/wechat-desktop.md.
#
# Before `docker compose up -d`:
#   openssl rand -hex 32 > agent-wechat-token && chmod 600 agent-wechat-token
#   cp .env.example .env   # fill in from Studio -> Channels -> Custom (wechat-desktop)
#
# The runtime source is vendored as a standalone build root in
# brian-message-store/agent-wechat. AGENT_WECHAT_SOURCE defaults to the sibling
# checkout layout used by the operator docs; set it when the repositories live
# elsewhere. AGENT_WECHAT_IMAGE is the local/pinned output tag.

services:
  agent-wechat:
    image: ${AGENT_WECHAT_IMAGE:-use-brian/agent-wechat:local}
    build:
      context: ${AGENT_WECHAT_SOURCE:-../../../brian-message-store/agent-wechat}
      dockerfile: docker/Dockerfile
    security_opt:
      - seccomp=unconfined
    cap_add:
      - SYS_PTRACE
    volumes:
      - agent-wechat-data:/data
      - agent-wechat-home:/home/wechat
      - ./agent-wechat-token:/data/auth-token:ro
    # No host port: :6174 is reachable only on the compose network.
    restart: unless-stopped

  bridge:
    build:
      context: .
      args:
        GIT_SHA: ${GIT_SHA:-unknown}
        BUILD_TIME: ${BUILD_TIME:-unknown}
    env_file: .env
    environment:
      AGENT_WECHAT_URL: http://agent-wechat:6174
      BRIDGE_STATE_FILE: /data/bridge-state.json
      BRIDGE_PORT: "8086"
    depends_on:
      - agent-wechat
    volumes:
      - bridge-data:/data
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8086/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    restart: unless-stopped

volumes:
  agent-wechat-data:
  agent-wechat-home:
  bridge-data:
