{
  "name": "@use-brian/browser-extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.chromium.json && node scripts/assemble.mjs",
    "build:firefox": "rm -rf dist-firefox && tsc -p tsconfig.firefox.json && node scripts/assemble-firefox.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^20.19.39",
    "typescript": "^5.7",
    "vitest": "^3"
  }
}
