<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron needs JIT + writable executable memory under the hardened runtime. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Dock live recording (docs/architecture/media/live-capture.md): the
         hardened runtime blocks microphone capture without this entitlement,
         and the failure is silent (getUserMedia hangs/denies with no OS
         prompt). Pairs with NSMicrophoneUsageDescription in electron-builder's
         extendInfo. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
