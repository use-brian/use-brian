/**
 * Home dock — public surface.
 *
 * The "Suggested for you" home surface's shared contracts: live signals, the
 * curation artifact + schema, the merge, the store interface, and the
 * `setHomeDock` tool. See docs/architecture/features/home-dock.md.
 */

export * from './types.js'
export * from './merge.js'
export * from './tools.js'
